const test=require('node:test');const assert=require('node:assert/strict');const fs=require('node:fs');const path=require('node:path');const root=path.resolve(__dirname,'..');const html=fs.readFileSync(path.join(root,'index.html'),'utf8'),mg=fs.readFileSync(path.join(root,'management-engine-v8-22-0.js'),'utf8'),ce=fs.readFileSync(path.join(root,'commerce-engine-v8-22-0.js'),'utf8'),sw=fs.readFileSync(path.join(root,'service-worker.js'),'utf8'),central=fs.readFileSync(path.join(root,'central-modules-hotfix-v8-22-2.js'),'utf8');
test('Central Gerencial expõe todas melhorias aprovadas',()=>{for(const s of ['Cockpit do Gestor','Pricing Multicanal','iFood / 99 / Taxas','Alertas e Exceções','PCP + Compras Inteligentes','Financeiro Gerencial','Aprovações','Automações SE → ENTÃO','Lotes / FEFO / Perdas','Tarefas / Manutenção','Pergunte ao John','Saúde / Auditoria'])assert.match(html,new RegExp(s.replace(/[.*+?^${}()|[\]\\]/g,'\\$&')))});
test('Central Gerencial possui funções operacionais',()=>{for(const s of ['cashProjection','financialClosings','reconciliations','approvalRequests','runAutomation','traceability','mgRepairCatalog','approvePrice'])assert.match(mg,new RegExp(s))});
test('Motor Comercial expõe todas funções comerciais',()=>{for(const s of ['Upsell / Cross-sell','Combos Inteligentes','Cupons','Fidelidade','Capacidade','Carrinhos Abandonados','Relacionamento','Avaliações'])assert.match(html,new RegExp(s.replace(/[.*+?^${}()|[\]\\]/g,'\\$&')));for(const s of ['startAt','endAt','maxDiscount','FAVORITE','REORDER','syncEcommerce'])assert.match(ce,new RegExp(s))});
test('service worker usa a linha consolidada atual',()=>{assert.match(sw,/const CACHE='john-erp-pwa-v8\.22\.\d+[-\w]*'/);assert.match(sw,/commerce-engine-v8-22-0\.js/);assert.match(sw,/management-engine-v8-22-0\.js/);assert.match(sw,/central-modules-hotfix-v8-22-2\.js/);assert.match(sw,/transaction-persistence-v8-22-12\.js/)});
test('módulos comerciais/gerenciais ficam centrais e não laterais',()=>{assert.match(central,/\.sidebar \.john-module-group\[data-module="motorcomercial"\]/);assert.match(central,/\.sidebar \.john-module-group\[data-module="centralgerencial"\]/);assert.match(central,/display:none!important/);assert.match(central,/johnNext\?\.renderHub/)});
